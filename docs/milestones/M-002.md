# M-002 — Viridian Forest Nurse

**Status:** COMPLETE

## Objective

Close the bounded CFRU Viridian Forest Nurse integration without changing DPE,
UPR-FVX, or any other workspace component Gitlink.

## Candidate and scope

**CONFIRMED CURRENT STATE:** CFRU PR #36, `M-002: add Viridian Forest nurse`,
is merged into `compat/firered-gen9-randomizer` at
`9548877aa481750b825c765c4d72fce90d633c16`.

The tested candidate revision is
`98cc40664080f7c956edbb81769acb7d112ce2ee`. The workspace pins the actual
integrated CFRU merge revision
`9548877aa481750b825c765c4d72fce90d633c16`; these revisions are intentionally
recorded separately.

The CFRU candidate is limited to:

- replacing the original non-trainer at Viridian Forest `(29,58,3)` with a
  down-facing Nurse Joy;
- poison-gated, repeatable whole-party healing;
- Nurse prompt, success, decline, and poison-refusal dialogue; and
- fail-closed map-object replacement support and its focused self-test.

**CONFIRMED CURRENT STATE:** The poison-refusal text is exactly:

```text
Sorry, but I can't heal if
you have a poisoned Pokémon.
```

## Evidence

### Static gate

**CONFIRMED CURRENT STATE:** The CFRU candidate passed its existing
map-object-overlay self-test, Python syntax check, diff check, and scoped
source/text inspection. The overlay contract validates the original object
before replacing it and preserves the map's object count plus its warp,
coordinate-event, and background-event pointers.

### Runtime gate

**CONFIRMED CURRENT STATE — user-supplied sanitized PASS for tested candidate
`98cc40664080f7c956edbb81769acb7d112ce2ee`:**

- Nurse is visible and usable.
- Healing works.
- Poison blocks healing.
- The corrected poison-refusal text displays correctly.
- Prompt and success text display correctly.

The result is targeted runtime evidence only. It is not a broad playthrough,
target-emulator compatibility claim, or stable support-profile promotion.
The integrated merge commit `9548877aa481750b825c765c4d72fce90d633c16` is not
claimed to have been separately runtime-tested.

## Integration boundary

- Only `02_external/CFRU-expansion` changes Gitlink in this closure.
- DPE and UPR-FVX Gitlinks remain unchanged.
- No ROMs, saves, emulator states, builds, tool binaries, private paths,
  screenshots, tokens, secrets, or `.env` files are included.
- The workspace closure PR remains a draft and must not be merged by Codex.

## Stop conditions

Stop for any additional Gitlink change, a divergence from CFRU merge revision
`9548877aa481750b825c765c4d72fce90d633c16`, missing/conflicting evidence, or
a need to touch a protected artifact or another component.

## Definition of done

The workspace draft PR pins only the exact integrated CFRU M-002 merge
revision, preserves the tested-candidate/runtime-evidence distinction, and
leaves the workspace PR unmerged for review.
