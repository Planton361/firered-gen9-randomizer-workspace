# M-007 — Shortened Oak Parcel Flow

**Status:** COMPLETE

## Objective

Close the bounded CFRU Shortened Oak Parcel Flow integration without changing
DPE, UPR-FVX, M-005 Oak's Lab Potion, M-006 Mom/start flow, the original
Route 1 Potion Clerk, or unrelated maps and story scenes.

## Candidate and scope

**CONFIRMED CURRENT STATE:** CFRU PR #41, `feat: shorten Oak Parcel flow`, is
integrated into `compat/firered-gen9-randomizer` at
`62298cf81d4a2b487c8793bad8b6e29906c705f4`.

The runtime-tested candidate revision is
`00e316532be65f4fdd8e4561b2b2579817a1f64f`. The integrated merge is one
commit ahead of that candidate and has no additional file changes. The
workspace pins the actual integrated revision
`62298cf81d4a2b487c8793bad8b6e29906c705f4`; these revisions are intentionally
recorded separately.

The CFRU change moves the Parcel handoff to the bounded Route 1 Clerk and
Pallet Town Oak sequence, while preserving the original Route 1 Potion Clerk,
the project's Pokedex/National Dex policy, and the protected earlier milestone
flows.

## Evidence

### Runtime gate

**CONFIRMED CURRENT STATE — user-supplied sanitized PASS for tested candidate
`00e316532be65f4fdd8e4561b2b2579817a1f64f`:**

- The M-006 Mom/start/starter/Rival flow remains functional.
- The temporary Pallet parcel Oak is hidden before Parcel acquisition, and the
  original Route 1 Potion Clerk remains functional.
- The Route 1 Parcel handoff works from all four trigger positions; the Parcel
  is awarded exactly once, the temporary Clerk disappears, and save/reload
  preserves the expected state.
- Viridian Mart does not duplicate the Parcel handoff.
- The Pallet Oak return handoff works from both trigger positions; Oak removes
  the Parcel, grants Pokedex/unlock state and five Poke Balls, and remains gone
  afterward.
- The post-Parcel Viridian Mart state is normal; the Old Man no longer blocks
  the road and the catching tutorial is skipped; Daisy/Town Map and Route 22
  post-Parcel state are correct.
- Oak's Lab does not replay the vanilla Parcel scene, and the M-005 Potion and
  M-006 flow remain intact.

The result is targeted runtime evidence only. It is not a broad playthrough,
target-emulator compatibility, Ironmon Tracker compatibility, randomizer
behavior beyond the recorded checks, or a stable support-profile claim. The
integrated merge commit `62298cf81d4a2b487c8793bad8b6e29906c705f4` is not
claimed to have been separately runtime-tested.

## Integration boundary

- Only `02_external/CFRU-expansion` changes Gitlink in this closure.
- DPE and UPR-FVX Gitlinks remain unchanged.
- No ROMs, saves, emulator states, builds, tool binaries, private paths,
  hashes, screenshots, tokens, secrets, or `.env` files are included.
- The workspace closure PR remains a draft and must not be merged by Codex.

## Stop conditions

Stop for any additional Gitlink change, a divergence from CFRU merge revision
`62298cf81d4a2b487c8793bad8b6e29906c705f4`, missing/conflicting evidence, or
a need to touch a protected artifact or another component.

## Definition of done

The workspace draft PR pins only the exact integrated CFRU M-007 merge
revision, preserves the tested-candidate/runtime-evidence distinction, and
leaves the workspace PR unmerged for review.
