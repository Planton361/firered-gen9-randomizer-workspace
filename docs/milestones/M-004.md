# M-004 — Guaranteed Renewable Step Items

**Status:** COMPLETE

## Objective

Close the bounded CFRU Guaranteed Renewable Step Items integration without
changing DPE, UPR-FVX, item IDs, hidden-item BG events, or any other workspace
component Gitlink.

## Candidate and scope

**CONFIRMED CURRENT STATE:** CFRU PR #38, `M-004: guarantee renewable step
items`, is integrated into `compat/firered-gen9-randomizer` at
`520fc7feeb7494b5f8f0555e348c13f0e847304b`.

The runtime-tested candidate revision is
`aad9c76d537cf812673e1cd3e69faffd435ff692`. The integrated merge is one
commit ahead of that candidate and has no additional file changes. The
workspace pins the actual integrated revision
`520fc7feeb7494b5f8f0555e348c13f0e847304b`; these revisions are intentionally
recorded separately.

The CFRU change preserves FireRed's 1,500-step renewable-item cycle, makes
eligible Underground Pass and approved Sevii renewable groups non-empty, and
leaves Mt. Moon's vanilla/random behavior unchanged. It retains the original
hidden-item events and therefore preserves whichever item UPR-FVX placed in an
eligible slot.

## Evidence

### Runtime gate

**CONFIRMED CURRENT STATE — user-supplied sanitized PASS for tested candidate
`aad9c76d537cf812673e1cd3e69faffd435ff692`:**

- Underground Pass guaranteed renewable spawn works.
- The Underground Pass repeat regeneration cycle works.
- A representative Sevii renewable group works.
- Mt. Moon remains the vanilla/random control and is not guaranteed.
- Itemfinder works.
- Normal one-time hidden-item behavior remains unchanged.
- A representative UPR-FVX Field Items smoke preserves randomized item
  semantics.

The result is targeted runtime evidence only. It is not a broad playthrough,
target-emulator compatibility, or stable support-profile claim. The integrated
merge commit `520fc7feeb7494b5f8f0555e348c13f0e847304b` is not claimed to have
been separately runtime-tested.

## Integration boundary

- Only `02_external/CFRU-expansion` changes Gitlink in this closure.
- DPE and UPR-FVX Gitlinks remain unchanged.
- No ROMs, saves, emulator states, builds, tool binaries, private paths,
  hashes, screenshots, tokens, secrets, or `.env` files are included.
- The workspace closure PR remains a draft and must not be merged by Codex.

## Stop conditions

Stop for any additional Gitlink change, a divergence from CFRU merge revision
`520fc7feeb7494b5f8f0555e348c13f0e847304b`, missing/conflicting evidence, or
a need to touch a protected artifact or another component.

## Definition of done

The workspace draft PR pins only the exact integrated CFRU M-004 merge
revision, preserves the tested-candidate/runtime-evidence distinction, and
leaves the workspace PR unmerged for review.
