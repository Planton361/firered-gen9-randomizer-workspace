# M-005 — PC Item -> Oak's Lab

**Status:** COMPLETE

## Objective

Close the bounded CFRU PC Item -> Oak's Lab integration without changing DPE,
UPR-FVX, starter species or balls, Pallet story flow, or any other workspace
component Gitlink.

## Candidate and scope

**CONFIRMED CURRENT STATE:** CFRU PR #39, `M-005: PC Item -> Oak's Lab`, is
integrated into `compat/firered-gen9-randomizer` at
`4a9698467600500d18ec8c08f9269f0d6ad008e6`.

The runtime-tested candidate revision is
`10a338eca514c3ca1f614586d1054a89f039010a`. The integrated merge is one
commit ahead of that candidate and has no additional file changes. The
workspace pins the actual integrated revision
`4a9698467600500d18ec8c08f9269f0d6ad008e6`; these revisions are intentionally
recorded separately.

The CFRU change keeps New Game Player PC initialization clearing all PC item
slots without adding the starter Potion, and adds the Potion as a normal,
one-time visible Item Ball in Professor Oak's Lab. Its standard finditem path
preserves normal UPR-FVX Field Items discovery and rewriting semantics.

## Evidence

### Runtime gate

**CONFIRMED CURRENT STATE — user-supplied sanitized PASS for tested candidate
`10a338eca514c3ca1f614586d1054a89f039010a`:**

- A fresh New Game has no starter Potion in PC storage.
- The Oak's Lab Potion Item Ball works.
- Pickup disappearance and persistence work.
- Starter, Rival, Oak's Lab flow, and warps remain intact.
- Normal Player PC storage remains functional.
- Representative UPR-FVX Field Items behavior remains compatible.

The result is targeted runtime evidence only. It is not a broad playthrough,
target-emulator compatibility, or stable support-profile claim. The integrated
merge commit `4a9698467600500d18ec8c08f9269f0d6ad008e6` is not claimed to have
been separately runtime-tested.

## Integration boundary

- Only `02_external/CFRU-expansion` changes Gitlink in this closure.
- DPE and UPR-FVX Gitlinks remain unchanged.
- No ROMs, saves, emulator states, builds, tool binaries, private paths,
  hashes, screenshots, tokens, secrets, or `.env` files are included.
- The workspace closure PR remains a draft and must not be merged by Codex.

## Stop conditions

Stop for any additional Gitlink change, a divergence from CFRU merge revision
`4a9698467600500d18ec8c08f9269f0d6ad008e6`, missing/conflicting evidence, or
a need to touch a protected artifact or another component.

## Definition of done

The workspace draft PR pins only the exact integrated CFRU M-005 merge
revision, preserves the tested-candidate/runtime-evidence distinction, and
leaves the workspace PR unmerged for review.
