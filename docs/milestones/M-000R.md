# M-000R — Workflow Adoption

**Status:** COMPLETE

**Baseline merge SHA:** `65da597b6b57d40d4698a221809741912abe8e3c`

## Objective

Establish one concise repository-backed workflow baseline while preserving
existing evidence and technical architecture.

## Non-goals

No product/source work, gameplay change, TM/HM rollout promotion, randomizer
campaign, architecture modernization, dependency migration, CI, container,
repository rename, or submodule Gitlink change.

## Allowed files

`AGENTS.md`, `README.md`, `.github/pull_request_template.md`, `docs/**`, the
named legacy workflow pointers, and
`07_scripts/bootstrap/check_git_safety.py`.

## Acceptance criteria

- Canonical `docs/` structure and short `AGENTS.md` exist.
- Historic workflow files point to canonical policy without deleting history.
- Python safety check is standard-library-only, POSIX runnable, fails on
  forbidden tracked/status paths, and refuses `main` by default.
- README and PR template align to the new baseline.
- No product file, submodule Gitlink, protected artifact, or suspended PR is
  modified or promoted.

## Stop conditions

Stop for work on `main`, unexpected changes outside the known suspended CFRU
checkout, a protected artifact, Gitlink mutation, need for product work, or an
unresolvable product conflict.

## Suspended in-flight PRs

CFRU PR #35 and Workspace PR #467 are suspended, not canceled. Do not modify,
merge, or update their Gitlinks in this milestone.

## Definition of Done

Canonical links resolve to tracked files; focused Python checks, safety check,
diff check, status/stat, and submodule/Gitlink review pass; the documentation
commit is pushed on the approved branch with a Draft PR and body file.
