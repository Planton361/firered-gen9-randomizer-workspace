# M-013 — Premier Ball purchase bonus

Status: **DRAFT SOURCE CANDIDATE; NOT RUNTIME ACCEPTED OR INTEGRATED.**
This supersedes M-012's future-work status for this bounded gap only; no other
M-012 candidate is promoted or implemented.

Workspace origin/main baseline `7437cd551545ab5e4dcd57a2ff5dbf9672193d74`,
pinned CFRU `827fa1ef04bd43e5c6bad5c47f7d8690ea6823ec`. CFRU has no main
branch; use the current workspace pilot pin / compatibility branch, not the
unrelated default Experiments head. Existing checkouts and Gitlinks preserved.

## Discovery and source-backed contract

- [Cyan NatDex shop](https://github.com/CyanSMP64/pokefirered/blob/16b8b9ffd77607debe7ce332cd50d3615f47e125/src/shop.c#L1004)
  gives floor(purchased quantity / 10) Premier Balls for any ball-pocket item,
  per transaction, clamped to remaining capacity. No carry between purchases.
  Thus 9→0, 10→1, 19→1, 20→2, 21→2; separate 9+9 still gives zero.
- [pret shop](https://github.com/pret/pokefirered/blob/e060ab955b5dc9ac1c4904c2cd141683615cf477/src/shop.c#L1007)
  returns to the item list without this bonus.
- Pinned CFRU has both multiple-Premier and custom-purchase rewards enabled.
  Its custom table runs first (Great→Dusk, Ultra→Luxury), intercepting the
  desired Premier rule; the fallback also lacks explicit partial-capacity
  handling and truncates the task quantity to u8.

End-of-session public-source recheck: Cyan natdex head
`a1a114e70f685f808b691fbb9e1f87ecf475eebc` and pret master
`c75f352304d529f6ba92d4f74b9cf8b5c3810788` retain these same callback rules.
Only source Git objects were fetched/read; reference pins/checkouts are unchanged.

**Code required.** Smallest bounded change: prioritize only ball-pocket Premier
calculation in the existing callback, use the full task quantity for that
calculation, decrease reward until target CheckBagHasSpace accepts it, add once,
and return without custom-ball fallback even if zero fits. Preserve non-ball
table logic including legacy u8 quantity behavior. Target BPRE Bag API remains
the capacity authority; no bag/shop refactor or guessed free-space scan.

## Candidate / review

[CFRU draft #45](https://github.com/Planton361/CFRU-expansion/pull/45), final
candidate `d8468e1d12dbe33f646e2778bbde51ece7010a73`, base
`compat/firered-gen9-randomizer`. Full implementation/tests/runtime matrix:
[candidate M-013 handoff](https://github.com/Planton361/CFRU-expansion/blob/d8468e1d12dbe33f646e2778bbde51ece7010a73/docs/M-013.md).

No DPE, UPR-FVX, Move Reminder, item IDs/prices/inventory or other shop work.
M-009 preflight permits only the purchase callback and its test, while retaining
all existing global-frame guards. The independent five-learnset repair in
[CFRU #46](https://github.com/Planton361/CFRU-expansion/pull/46) is stacked on
this branch, not included in M-013's bounded PR diff.

PASS: real-callback host test (11,110 ball quantity/capacity/A-B cases plus
failure, repeat-input, transaction and non-ball boundary controls), M-009 source
contract/eight negative cases/compiled scanner, ARM item.c syntax, whitespace
and workspace safety checks. Clean full source builds were attempted on M-013
and the final stacked candidate: C/assembly/strings/images completed, audio
failed because wav2agb is absent; mid2agb is also unavailable. Full build/link
and all runtime tests remain open. No ROM insertion/protected artifact access.

User-only gates: full source build; ordinary/randomized shop quantities and
ball types; separate transactions; Premier self-purchase; partial/full stacks
and pockets; actual purchase consumes final capacity; cancellation/insufficient
funds/failure/repeated input; non-ball controls; re-entry/save-reload. Expected
amount is always min(floor(qty/10), post-purchase available Premier capacity).

Do not merge automatically. After user acceptance/integration, record actual
merge/tested SHAs and update only the intended workspace pin in a separate PR.
Continue independent work while runtime, merge and protected-artifact gates wait.
