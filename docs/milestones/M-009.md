# M-009 — Hidden Item Sparkle / source-owned Overworld frame integration

**Status:** COMPLETE

**Evidence classification:** CONFIRMED CURRENT STATE, with targeted runtime caveat.

## Revisions and integration

[CFRU PR #43](https://github.com/Planton361/CFRU-expansion/pull/43) is merged
into `compat/firered-gen9-randomizer`.

| Role | CFRU revision |
|---|---|
| Previous workspace baseline (M-008) | `a869c3526d7f76c54082bc71e236742564319e02` |
| Runtime-tested / user-accepted candidate | `3da0547d782fb62fc993431278d63575912025da` |
| Integrated merge / new workspace pin | `827fa1ef04bd43e5c6bad5c47f7d8690ea6823ec` |

Git verification confirms the merge is exactly one commit ahead of the
candidate, the candidate is their merge base, and candidate-to-merge has no
additional file changes. The integrated merge revision was **not separately
runtime-tested**. Identical files do not turn that merge into a separate test.

## Source ownership

The old slow-camera `OverworldBasic` byte owner was intentionally superseded.
The approved change restores Quest Log arrival processing and the canonical
`BuildOamBuffer` position. A single named `functionrewrites` entry now directs
both original CB2 wrappers to `M009_OverworldBasic`; the wrappers' fade/VBlank
behavior is untouched. The old active byte replacement and obsolete listing
were removed together, with rollback provenance retained in CFRU source docs.

The exact source-owned sequence is:

```text
ScriptContext_RunScript
RunTasks
AnimateSprites
CameraUpdate
SetQuestLogEvent_Arrived
UpdateCameraPanning
BuildOamBuffer
UpdatePaletteFade
UpdateTilesetAnimations
DoScheduledBgTilemapCopiesToVram
TryStartVisibleHiddenItemSparkles
```

The separate scanner runs at the frame tail, after camera processing and
scheduled BG copies. It uses normal `BG_EVENT_HIDDEN_ITEM` data from the
current map, filters collected flags, `ITEM_NONE` and underfoot cases, and
uses camera focus, a visible window and bounded per-BG-event cooldowns.
Existing transient `FLDEFF_SPARKLE` supplies the effect. No persistent custom
Sprite or palette ownership exists; no UPR-FVX dependency or pickup rewrite
was introduced.

Source details and rollback are pinned in
[CFRU's M-009 implementation record](https://github.com/Planton361/CFRU-expansion/blob/827fa1ef04bd43e5c6bad5c47f7d8690ea6823ec/docs/M-009.md).
That record's pre-acceptance runtime-pending wording describes the candidate
handoff; the bounded user acceptance below is the subsequent closure evidence.

## Runtime evidence and caveat

**User-supplied result: `PASS_TARGETED_VISIBLE_SPARKLE_WITH_CAVEATS`.**

Hidden Item sparkle behavior was observed in runtime and visually judged
appropriate on the exact candidate
`3da0547d782fb62fc993431278d63575912025da`.

The [sanitized runtime record](../../08_tests/randomizer/m009_hidden_item_sparkle_runtime_acceptance.md)
contains the supported statement and its limits. It does not assert that
every Start/Bag/Party/Quest-Log/resource-pressure/lifecycle permutation was
separately tested. Broader global-frame regressions, transitions/warps,
save/reload, menu returns, multiple effects and resource pressure remain in
the later feature-complete playthrough gate. No broad support, release,
target-emulator or Tracker compatibility claim is made.

Earlier [failed sparkle pilot/revert evidence](../../08_tests/randomizer/cfru-hidden-item-sparkle-qol.md)
remains **LEGACY / OBSOLETE**. Those failures are not retroactively promoted
to PASS, and their old source-hook blocker no longer describes M-009.

## Workspace closure boundary

This closure starts from fresh workspace main
`172902c4eec9efe5fe26a7aff1b1d81b6edc4d66` on a separate milestone branch.
Only `02_external/CFRU-expansion` advances Gitlink, from the M-008 baseline to
the integrated M-009 merge. DPE, UPR-FVX, Ironmon Tracker, NatDexExtension
and all other component pins remain unchanged. No product source is edited.

Verification: Git safety check, `git diff --check`, exact revision relationship,
sole-Gitlink scope, artifact safety, and canonical status consistency. No
build, ROM, save, emulator-state or generated-offset inspection is part of
this closure, and no new runtime test is claimed. The workspace PR remains
a draft for review; Codex must not merge it.

The next major track is **Controlled Gen 1–9 Data Risk Closure**, not started
by this PR. Friendship Boost remains optional and non-standard for Ironmon.
