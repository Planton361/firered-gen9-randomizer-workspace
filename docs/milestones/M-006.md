# M-006 — Talk to Mom / Faster New Game handoff

**Status:** COMPLETE

## Objective

Close the bounded CFRU Talk to Mom / Faster New Game handoff integration
without changing DPE, UPR-FVX, starter selection, Rival selection or battle,
the M-005 Oak's Lab Potion, later Oak's Lab scenes, or unrelated maps.

## Candidate and scope

**CONFIRMED CURRENT STATE:** CFRU PR #40, `M-006: Talk to Mom / Faster New
Game handoff`, is integrated into `compat/firered-gen9-randomizer` at
`237fc61ac52bea6978f4b434c06fc3f1f11e5dcc`.

The final runtime-tested candidate revision is
`574318a1af2801c161cd40d9687ed2b65dd3b92a`. The integrated merge is one
commit ahead of that candidate and has no additional file changes. The
workspace pins the actual integrated revision
`237fc61ac52bea6978f4b434c06fc3f1f11e5dcc`; these revisions are intentionally
recorded separately.

The CFRU change requires talking to Mom before leaving Player House 1F, then
uses the bounded fast scene-1 Oak's Lab handoff. It preserves the starter-ball
and Rival machinery, later Lab scenes, and Mom's post-rival healing path.

## Evidence

### Runtime gate

**CONFIRMED CURRENT STATE — user-supplied sanitized PASS for final tested
candidate `574318a1af2801c161cd40d9687ed2b65dd3b92a`:**

- A fresh New Game cannot leave Player House before talking to Mom; the exit
  blocker works.
- Mom's magic-trick handoff works.
- The fast Oak's Lab scene has the correct player and camera state, with Oak
  at `(6,3)`, facing down.
- The player reaches direct starter choice; starter selection and the Rival
  starter/battle work.
- Oak's Lab exit and warps work, and the M-005 Potion remains functional.
- Mom's post-rival healing works.

Earlier M-006 candidates had reported runtime defects and were superseded by
the final accepted candidate above. They are not promoted to PASS evidence.

The result is targeted runtime evidence only. It is not a broad playthrough,
target-emulator compatibility, or stable support-profile claim. The integrated
merge commit `237fc61ac52bea6978f4b434c06fc3f1f11e5dcc` is not claimed to have
been separately runtime-tested.

## Integration boundary

- Only `02_external/CFRU-expansion` changes Gitlink in this closure.
- DPE and UPR-FVX Gitlinks remain unchanged.
- No ROMs, saves, emulator states, builds, tool binaries, private paths,
  hashes, screenshots, tokens, secrets, or `.env` files are included.
- The workspace closure PR remains a draft and must not be merged by Codex.

## Stop conditions

Stop for any additional Gitlink change, a divergence from CFRU merge revision
`237fc61ac52bea6978f4b434c06fc3f1f11e5dcc`, missing or conflicting evidence,
or a need to touch a protected artifact or another component.

## Definition of done

The workspace draft PR pins only the exact integrated CFRU M-006 merge
revision, preserves the final-tested-candidate/runtime-evidence distinction,
and leaves the workspace PR unmerged for review.
