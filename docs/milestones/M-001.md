# M-001 — 29-slot TM/HM Itemball Acceptance / Integration Restart

**Status:** ACCEPTANCE_DECISION_PENDING

## Objective

Restart the approved 29-slot TM/HM itemball candidate under the M-000R
workflow baseline, establish candidate-specific acceptance evidence, and make
an explicit integration decision only after all required gates pass. The
technical acceptance evidence is complete; this milestone does not merge CFRU
PR #35 or claim final product integration without that separate authorization.

## Candidate and scope

**CONFIRMED CURRENT STATE:** CFRU PR #35 is the active CFRU product candidate
at `08b869032735118539411adbcffa421c8a697caa`. The workspace candidate pin is
that exact commit.

**LEGACY / OBSOLETE:** Workspace PR #467 is closed unmerged. Its branch and
commit are supporting workflow handoff evidence only, not the active
integration PR.

**CONFIRMED CURRENT STATE:** The accepted scope is 28 TMs plus HM07. Every
whitelisted visible `finditem` object uses graphics ID `0x065C`, retaining low
byte `92` for UPR-FVX discovery. Item value, script, flag, pickup behavior, and
HM07 preserve-only policy remain unchanged.

## Gates

### Static gate

**CONFIRMED CURRENT STATE:** The candidate has passed its approved multi-row
overlay whitelist, target/control expectations, upper-byte-only serialization,
generator check, diff/Python/link checks, and clean insertion evidence. The
candidate pin and its documented static contract must remain intact.

### Runtime gate

**CONFIRMED CURRENT STATE:** **PASS.** User-reported sanitized evidence for
the exact candidate confirms every representative case: TM09 and its normal
Potion control in both pickup orders, TM05, elevation-0 TM41, HM07, elevation-0
Sevii TM36, and a normal red/white item-ball control. The report confirms the
required colors, reward/pickup and removal behavior, map re-entry, in-game
save/reload, and no reported sprite/tile/palette or map/event regression.

### UPR-FVX gate

**CONFIRMED CURRENT STATE:** **PASS.** User-supplied sanitized evidence for
the exact candidate records PASS for all six fresh Field Item save/reload rows:
Unchanged/off, Shuffle/off, Random/off, Random/on, Random Even/off, and Random
Even/on. Every row reports `candidateLoaded=true`, `saveSuccessful=true`,
`reloadSuccessful=true`, `rawApiTmSlotAlignmentMismatches=0`,
`tmFieldItemSlotMismatches=0`, `nonTmFieldItemSlotMismatches=0`,
`requiredFieldTMMissingAfter=0`, `fieldItemReloadMismatches=0`, and
`lowByte92Discovery=true`.

**CONFIRMED CURRENT STATE:** All M-001 technical acceptance gates have passed.
The explicit acceptance/integration decision remains pending and is outside
this evidence-recording task.

## Integration order

1. Preserve the reviewed workspace CFRU Gitlink at the exact candidate commit.
2. Preserve the passed static contract and representative runtime evidence.
3. Preserve the passed six-row candidate-specific UPR-FVX evidence.
4. Obtain an explicit user-authorized acceptance/integration decision for CFRU
   PR #35 and the workspace pin. Do not merge CFRU PR #35 from this milestone
   branch.

## Stop conditions

Stop for a candidate-pin mismatch, any other Gitlink change, a static-contract
failure, missing or conflicting runtime evidence, a failed UPR-FVX row, product
source work, protected artifacts, or a need to merge either product PR.

## Definition of Done

The exact candidate pin is reviewed; the static contract remains valid; named
representative runtime slots and all six fresh UPR-FVX rows meet their defined
criteria; evidence is revision-specific and sanitized; and any acceptance or
integration decision is documented without silently promoting support. The
technical criteria are satisfied, while the explicit acceptance/integration
decision remains pending.
